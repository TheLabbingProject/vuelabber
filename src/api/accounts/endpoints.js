import BASE from '@/api/base_url.js'

const ACCOUNTS_BASE = `${BASE}/accounts`
const USERS = `${ACCOUNTS_BASE}/user`
const PROFILES = `${ACCOUNTS_BASE}/profile`
const LABS = `${ACCOUNTS_BASE}/laboratory`

export { PROFILES, LABS, USERS }
